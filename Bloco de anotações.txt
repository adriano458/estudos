comandos:

CD (local) --> cd descktop/
//Esse comanado irá abrir o local descktop que é a pasta.

mkdir (nome da pasta) && cd (entrar na pasta) --> mkdir exercicios-js && cd exercicios-js
//mkdir irá criar uma pasta.
//&& junção ou conjunto do comando.
//cd entrar na pasta que foi criada.

code .
//Acessar o vscode, com chamando a pasta que foi criada.

''Estudar o padrão observer para o javascript''


//Configurar nome:
git config --global user.name "José Adriano"

//Configrar e-mail:
git config --global user.email "ja.458@hotmail.com"

//Clonar repositorio no github
git clone (http do repositorio)

//listar arquivos não monitorados. Primeiro passo
git status

//Adicionar arquivos não monitorados
git add . (ponto significar adicionar tudo)

//Instalação do express (Tem que está dentro da pasta do projeto)
npm i --save express@4.16.2 -E (@4.16.2 --> é a versão exata que deseja instalar e o -E utilizado para não ter nenhuma modificação da instalação).

//Comitar para monitoramento das alterações
git commit -m "(descrição)"

//Retirar o arquivo de monitoramento
git restore (nome do arquivo) 

//Enviar os arquivos para o repositório
git push origin (main ou master)

Atualizar os arquivos na maquina que está no repositório
git pull origin (main ou  master)

//Verificar a listar de configurações do Git
git config --list

//Após criar uma pasta, sempre inicilizar.
git init

//Verificar o que está monitorando
ls -la

//Verificar o branch que esta mexendo (main ou master)
git branch

//baixar os arquivos node modules, diretamente no terminal git ou Code, dentro da pasta que deseja o download.
npm i lodash

// Usando o nodemon que executa o node no terminal do Windows ou code.
npm i -g nodemon
// para executar o arquivo.
nodemon "arquivo.js"

//Criar arquivo package.json direto no terminal
npm init

//Caso não queira preecher, irá marcar yes para todos
npm init -y

//criando dependencias
npm i --save axios