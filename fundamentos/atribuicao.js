const a = 7
let b = 3

b += a // Atribuição aditiva. b = b + a
console.log(b)

b -= 4 //Atribuição subtação. b = b - 4
console.log(b)

b *= 2 //Atribuição Multiplicativa. b = b * 2
console.log(b)

b /= 2 //Atribuição divisoria. b = b / 2
console.log(b)

b %= 2 // Modular. b = b % 2
console.log(b)
